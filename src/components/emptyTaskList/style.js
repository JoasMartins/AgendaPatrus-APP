import {StyleSheet} from "react-native"

const style = StyleSheet.create({
    container: {
        alignItems: "center",
        padding: 5,
    },
    text: {
        color: "#D2D2D2"
    }
})

export default style